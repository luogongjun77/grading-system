# 智能阅卷系统 - 下载安装指南

## 📦 下载到F盘

1. 将整个 `grading-system` 文件夹复制到 `F:\grading-system`
2. 文件夹结构：
```
F:\grading-system\
├── 启动阅卷系统.bat      ← 双击启动（电脑和手机局域网访问）
├── 手机远程访问.bat      ← 双击启动（手机用流量也能访问）
├── 数据备份.bat          ← 备份/恢复/导出数据
├── app.py               ← 主程序
├── requirements.txt     ← 依赖清单
├── export_data.py       ← 数据导出工具
├── templates/           ← 页面模板
├── static/              ← 静态文件
└── instance/            ← 数据库（自动生成）
```

---

## 🖥️ 电脑上如何打开

### 方法：双击 `启动阅卷系统.bat`

1. 双击 `启动阅卷系统.bat`
2. 首次运行会自动安装依赖（约1-3分钟）
3. 看到以下提示即启动成功：
```
  🖥️  电脑访问：http://127.0.0.1:5000
  📱 手机访问：http://192.168.x.x:5000
  管理员：admin / admin123
```
4. 打开浏览器，输入 `http://127.0.0.1:5000` 即可使用

> ⚠️ 首次需要安装 Python（见下方"前置要求"）

---

## 📱 手机如何访问（同一WiFi）

### 前提条件
- 手机和电脑连接**同一个WiFi**
- 电脑上的阅卷系统已启动

### 步骤
1. 电脑上双击 `启动阅卷系统.bat`
2. 记住提示中的📱手机访问地址，如 `http://192.168.1.100:5000`
3. 手机浏览器输入该地址即可

> 💡 手机访问看到的是同一个系统，数据完全同步！
> 电脑录入成绩，手机立刻能查到。

---

## 📱 手机远程访问（不同WiFi/用流量）

如果手机和电脑不在同一WiFi，使用 `手机远程访问.bat`：

1. 双击 `手机远程访问.bat`
2. 选择方式2（Cloudflare穿透，免费免注册）
3. 等几秒会生成一个公网链接，如：
   ```
   https://xxx-yyy-zzz.trycloudflare.com
   ```
4. 手机浏览器打开该链接即可

> ⚠️ 此方式需要电脑保持开机和联网

---

## 🔄 同步更新说明

**手机和电脑看到的是同一份数据**，因为：
- 手机访问的是电脑上运行的服务
- 数据存储在电脑的 `instance/grading.db` 文件中
- 所有操作实时生效，无需手动同步

### 多台电脑同步
如果需要在多台电脑间同步数据：
1. 使用 `数据备份.bat` 导出备份
2. 将 `backups/` 文件夹和 `exports/` 文件夹复制到另一台电脑
3. 另一台电脑用 `数据备份.bat` 的"恢复数据"功能导入

---

## ⚙️ 前置要求

### 安装 Python（只需一次）

1. 访问 https://www.python.org/downloads/
2. 下载 Python 3.9 或更高版本
3. 安装时**务必勾选** `Add Python to PATH`
4. 安装完成后重启电脑

### 验证安装
打开命令提示符，输入：
```
python --version
```
能看到版本号即安装成功。

---

## 🔑 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |

> ⚠️ 首次登录后请立即修改密码！

---

## 🛠️ 常见问题

### Q: 双击bat文件闪退
A: 右键bat文件 → "以管理员身份运行"

### Q: 手机打不开页面
A: 检查以下几点：
1. 手机和电脑是否在同一WiFi
2. 电脑防火墙是否阻止了5000端口
3. 尝试在电脑浏览器先打开 http://127.0.0.1:5000 确认服务正常

### Q: 防火墙放行
A: Windows搜索"防火墙" → 允许应用通过防火墙 → 添加 Python → 允许专用和公用网络

### Q: 端口被占用
A: 编辑 `app.py` 最后一行，将 5000 改为其他数字如 8080

### Q: 想关掉服务
A: 在运行bat的窗口按 `Ctrl+C`

---

## 📋 完整工作流程

```
1. 双击"启动阅卷系统.bat" → 打开浏览器
2. 登录管理后台 (admin/admin123)
3. 创建班级 → 批量录入学生
4. 创建考试 → 设置题目和分值
5. 设计答题卡 → 生成PDF → 打印发给学生
6. 收卷后扫描/拍照 → 批量上传答题卡（支持PDF）
7. AI辅助批改 → 教师确认分数
8. 查看成绩分析 → 导出CSV
9. 发布成绩 → 学生用手机查询
10. 用"数据备份.bat"定期备份
```
